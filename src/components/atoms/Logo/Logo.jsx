import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
  margin-left: 20vw;
`;

const Logo = () => (
  <Wrapper>
    <h1>Lemesh LOGO</h1>
  </Wrapper>
);

export default Logo;
